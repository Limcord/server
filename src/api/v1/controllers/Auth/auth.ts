import { Request, Response } from "express";
import { Controller } from "../../structures/Controller";

export class AuthController extends Controller("/auth/sessions") {
  "GET /"(req: Request, res: Response) {
    
  }
}

